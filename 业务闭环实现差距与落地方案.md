# 业务闭环实现差距与落地方案

> 目标闭环（OCR 分组归档之前的不管）：
> 1. 文档抽取进**电子病历夹**，证据/历史/溯源/红框正确显示，能列出并切换所有历史抽取值。
> 2. 能保存 **CRF 模版**，修改后产生新版本。
> 3. **科研项目**通过 CRF 模版 + 引入患者执行抽取，字段填充率高、溯源正确、历史值可切换。
> 4. **可重复表单**能正常合并并产生 `_2`/`_3` 实例。
> 5. 评估「在系统里塞入 Claude Code 架构」能否完成上述任务，并给出**任务队列 / 会话管理**方案。
>
> 本文基于对后端 services / repositories / api 与 frontend_new 的代码审查，区分
> 「已实现」「差哪些」「会影响闭环的 bug（带 文件:行）」「Claude Code 架构评估」「队列与会话方案」。
> 与既有的 `CLAUDE_AGENT_抽取流程问题清单.md`（A/B/C/D/E 编号）互补，本文引用其编号不再重复展开。

---

## 0. 结论速览

| 闭环能力 | 现状 | 关键阻塞 |
| --- | --- | --- |
| 病历夹抽取出结果 | 链路通，但吞吐/可靠性差 | C1 单并发队列、A2 全量重跑、A3 整表单作废 |
| 证据/红框显示 | **基本已实现**（后端富化 bbox + 前端 SVG 渲染 + 质量标签） | quote_text 与 OCR 源文不一致、page 尺寸缺失推断、record_shared 来源不清 |
| 历史值列出+切换 | **已实现**（events/candidates API + 前端时间线+采用按钮） | 并发选择无行锁、删除当前值后不自动降级 |
| 保存 CRF 模版+产生新版本 | **已实现**（每次保存 create_version + publish） | 版本号前端计算、发布后前端状态不同步 |
| 科研项目抽取+填充率 | 链路通 | **G2 换版本后旧患者 context 不迁移→填充率虚低/抽取写错版本**、D1 协作漏抽 |
| 可重复表单合并 `_2/_3` | 部分实现 | **F1 instance_label 两套格式不一致**、B1/B2 合并键脆弱导致碎片化 |

整体判断：**四条闭环的"主干"代码都已存在并能跑通"happy path"**，差的不是从零搭建，而是
①合并/版本/并发的正确性 bug，②Claude Code 封装带来的吞吐与可靠性问题（见 CLAUDE_AGENT 清单），
③若干边界与一致性收尾。下面逐项列出。

---

## 1. 电子病历夹抽取（含证据/红框/历史切换）

### 1.1 已实现（happy path 可跑通）
- 抽取链路：`update_patient_ehr_folder` → 规划 (文档×表单) job → claude_code 抽取 → `_write_extracted_values` 落库
  （`extraction_service.py:566`、`2244`）。
- 证据落库与红框：`evidence_location_resolver.resolve_evidence_locations` 把 quote_text 解析成
  page_no+polygon 并做质量分级（trusted/low_confidence/renderable=False），落到 `FieldValueEvidence.bbox_json`；
  前端 `HighlightedImage`、`PdfPageWithHighlight` 按 polygon 画 SVG 框，`EvidenceQualityTags` 显示「坐标仅供参考/仅页码」等降级提示。
- 历史值与切换：`FieldValueEvent`（append 事件流）+ `FieldCurrentValue`（当前选中，唯一约束 (context,record,field_path)）。
  API `GET .../fields/{field_path}/events`（全部历史）与 `/candidates`（候选）齐备；
  前端 `SchemaForm.jsx` 拉历史时间线 + 候选列表，「采用此值」调 `select-candidate` 持久化切换。

### 1.2 会影响闭环的 bug / 缺口（带定位）

- **H1 删除当前值后不自动降级到最新候选**：`structured_value_service.py` 的删除路径只删 `FieldCurrentValue`，
  不回退到次新候选 → 前端显示「暂无值」，但历史里其实还有候选。需在删除后自动 `select_current_value(最新候选)` 或前端提示。
- **H2 候选切换/并发抽取无行级锁**：`select_current_value`（`structured_value_service.py:79-125`）先 `get_by_field` 再 upsert，
  两个并发请求都可能读到「无当前值」后各自写入，后者覆盖前者。`record_ai_extracted_value` 的
  `auto_select_if_empty`（`:178-185`）同样有竞态。病历夹批量抽取（多文档并发写同一 context 的同名字段）会触发。
  → 用 `SELECT ... FOR UPDATE` 或复用 `_write_extracted_values` 已有的 `pg_advisory_xact_lock(context_id)`（见 B5）。
- **H3 重复抽取不自动更新当前值**（设计需明确）：首次抽取 auto-select，之后再抽到更好的值只进候选、不替换当前值
  （`:178-185`）。对「重新跑一遍病历夹希望刷新结果」的用户预期不符，需产品上确认或加「自动采用最高 confidence」策略。
- **H4 quote_text 与 OCR 源文不一致导致前端高亮失败**：`extraction_service.py:2499-2512` `_resolved_evidence_quote`
  多层 fallback（evidence.quote_text → field.quote_text → 显示值），可能与 `bbox_json.source_text` 不一致；
  前端 `FieldSourceViewer` 用 quote_text 在原文里高亮时找不到。红框（polygon）不受影响，但「原文高亮」会失效。
- **H5 record_shared 来源不清**：`extraction_service.py:2366-2367` 读 `evidence.record_shared`，但其赋值链路不明确，
  可能让「沿用同记录其它字段位置」的橙色框被误显示为高置信红框。需核对 `resolve_evidence_locations` 是否真的产出该标记。
- **H6 page_width/height 缺失时坐标靠推断**：`evidence_location_resolver.py` 缺页尺寸时取相邻页或 bbox 极值推断，
  前端 `HighlightedImage` 以 1100 为归一化/像素分界（`index.jsx:209` 附近）。非标准 OCR 页或 Claude 输出「伪归一化」坐标时红框会错位。
- 另见 CLAUDE_AGENT 清单 **C2**（completed-empty 文档增量更新被永久跳过）、**C4**（ocr_status=None 也抽）、
  **A3**（单字段不合格丢整表单）——这些直接造成「病历夹里某些字段始终抽不出/红框缺失」。

---

## 2. CRF 模版保存与版本化

### 2.1 已实现
- 模版三层模型：`SchemaTemplate` → `SchemaTemplateVersion`（`version_no` 唯一约束、status draft/published/deprecated）
  → `ProjectTemplateBinding`（绑定到**具体 version**，不是 template）。
- 保存即新版本：前端 `crfTemplate.js` 每次 `saveCrfTemplateDesigner` 计算 `nextVersionNo` 调
  `POST /schema-templates/{id}/versions` 建 draft，可选 `/publish`；后端 `schema_service.create_version`/`publish_version`
  从不 in-place 改已发布版本 → **「修改产生新版本」满足**。
- 项目快照模版：`research_project_service.bind_crf_template`（`:566-644`）绑定非快照模版时会
  `create_project_snapshot_template` 生成项目专属快照，隔离源模版后续变更。

### 2.2 会影响闭环的 bug / 缺口

- **G1 版本号由前端计算**：`crfTemplate.js` `nextVersionNo = max(versions)+1`，列表过期/不全时会撞
  `UniqueConstraint(template_id, version_no)` → 409。应由后端分配下一个 version_no（加 `/next-version-no` 或在 create_version 内部 `max+1`）。
- **G2 发布后前端状态不同步**：`publishCrfTemplate` 只返回 version 对象，不返回 template；
  `crfSlice.currentTemplate.status` 需二次拉取才更新，否则 UI 显示「未发布」。

### 2.3 与第 3 节强相关
模版「能产生新版本」没问题，真正的闭环风险在**新版本如何作用到已入组患者**——见 G3（下一节），
这是「改了模版但科研项目抽取仍按旧版本/填充率错乱」的根因。

---

## 3. 科研项目：CRF 模版 + 引入患者 → 抽取 → 填充率/溯源

### 3.1 已实现
- 绑定与引入：项目绑 `primary_crf`（`get_active_primary_crf`），`enroll_patient` 后
  `get_or_create_project_crf_context` 按 `binding.schema_version_id` 建 `project_crf` context 并播种默认 record。
- 抽取：`update_project_crf_folder` / `_batch` 走与 EHR 相同的 claude_code 抽取器，结果按 context 落库。
- 填充率：`research_project_service._compute_project_completeness`（`:186-259`）=
  「已填规范化字段 ∩ schema 叶子集合 / 叶子总数」，跨实例去重用 canonical path（去数字下标）。

### 3.2 会影响闭环的 bug / 缺口（**这里是科研项目最关键的坑**）

- **G3 换 CRF 版本后已入组患者的 context 不迁移 → 数据/填充率/抽取全部错位**（高危）：
  - 完整度计算 `research_project_service.py:371-378` 显式 `if context.schema_version_id != binding.version_id: continue`，
    旧版本 context 的患者被**整体跳过**，填充率显示 0 或缺失。
  - 抽取时 `get_or_create_project_crf_context` 找不到「新版本 context」就**新建一个空的**（`:743-757`），
    老数据留在旧 context，新抽取写进新 context → 用户看到「换版本后填充率清零、老溯源消失」。
  - → 需要：换版本时为已有患者**显式迁移/复制 context**（或允许跨版本聚合展示），并在 UI 明确版本切换影响。
- **G4 (project_patient, schema_version) 无唯一约束 + 先查后建**：`get_or_create_project_crf_context`
  并发或重复调用可能建出两个同版本 context（`research_project_service.py:736-761`）；完整度代码用「取 created_at 最新」
  容忍重复，侧面证明该状态会发生 → 抽取写一个、展示读另一个，「抽了看不到」。加唯一约束 + upsert/advisory 锁。
- **D1（见 CLAUDE_AGENT 清单）协作项目漏抽他人文档**：CRF 文档检索 `list_by_patient(..., uploaded_by=requested_by)`
  只拿本人上传的文档 → 多人协作项目填充率被系统性低估。项目场景应按成员/患者归属放开 uploaded_by。
- **G5 填充率把空 JSON/集合算未填**：`_is_value_filled` 对 `value_json in ([],{})` 判为未填，
  对「结构存在但无标量值」的字段会拉低填充率（影响展示，不影响数据）。

---

## 4. 可重复表单合并与 `_2 / _3` 实例

### 4.1 已实现
- 识别：schema `type=array && items.type=object`（前端 `CategoryTree.isRepeatableForm`），
  表单级 `x-is-extraction-unit`、字段级 `x-merge-binding`（`extraction_planner.py:74,82`、`schema_field_planner.py:228,245`）。
- 合并：`RecordInstanceMergeResolver.build_anchor_json` 用 merge_binding(anchor/fallback/group/interval/granularity)
  算 `merge_key`，相同 key 复用 RecordInstance，否则 `_next_repeat_index` 新建（`record_instance_merge.py:47-91,93-165`）。
- 前端：`CategoryTree` 展开实例列表、`RepeatableForm` 支持增删行、`FormPanel` 标题显示 `名称_序号`。

### 4.2 会影响闭环的 bug / 缺口

- **F1 instance_label 两套格式不一致（确认存在，且都是活代码）**（高优先，直接影响 `_2/_3` 显示）：
  - 合并器走的是下划线：`record_instance_merge.py:189` → `f"{title}_{repeat_index + 1}"`（即 `诊断_2`）。
  - 但**默认 record 播种**走的是空格井号：`ehr_service.py:751`、`research_project_service.py:1509`
    → `f"{form_title} #{repeat_index + 1}"`（即 `诊断 #2`）。
  - 前端统一按 `_序号` 生成显示（`FormPanel.jsx:603-613`、`CategoryTree.generateInstanceName`），
    所以**播种出来的实例和抽取合并出来的实例标签风格不一致**，同一可重复表单里会混着 `诊断` / `诊断 #2` / `诊断_3`。
  - → 统一为单一格式（建议都用 `_序号`，与前端对齐），并集中到一个工具函数。
  - 注：`extraction_service.py:2637` 的 `_create_output_record` 同样用 ` #` 格式，但 `_resolve_output_record`（`:2533`）
    **已无调用方**（grep 确认），属死代码，可删；真正生效的是上面两条播种路径。
- **F2 无 merge_binding / anchor 取空 → 碎片化或误并**（= CLAUDE_AGENT 清单 B1/B2，可重复表单这里后果最直接）：
  merge_key 退化为 `form|document=..|local_repeat_index=..`（`record_instance_merge.py:143-153`）：
  - 同文档内本应是一条的会因 local_repeat_index 不同被拆开；
  - 跨文档相同 anchor 值的本应合并的，因 document 不同而不并 → 出现一堆 `_2 _3 _4` 实际是同一条记录。
  靶向抽取只抽目标字段、不含 anchor 字段时更严重（CLAUDE_AGENT 清单 E2）。
- **F3 repeat_index 从 field_path 推断脆弱**：`local_repeat_index_for_field`（`record_instance_merge.py:236-253`）
  在 `repeat_index` 缺失时从路径数字段推断，路径用 `/` 或异常下标时推断错误 → 实例错位。
- **F4 填充率与碎片化叠加**：可重复表单碎片化后，若碎片实例字段值不一致，canonical 去重只记一次，
  实际填充率被低估（第 3.2 G5 同源）。

---

## 5. 「在系统里塞入 Claude Code 架构」能否完成闭环？

### 5.1 能（功能可达）
- 抽取所需的输入（OCR 文本、reading_units、schema、field_specs、merge_binding）已经齐备地喂给 Claude
  （`claude_code_runner._write_workspace_inputs`），输出经 `_validate_raw_result` + `_node_normalize`
  规范成既有字段候选格式，再走和 LLM 抽取器**完全相同**的落库/合并/证据链路（`_write_extracted_values`）。
  也就是说 Claude Code 只是替换了「抽取引擎」这一层，证据/历史/合并/红框等闭环能力**与引擎解耦**，不依赖它就能工作。
- skills（`agent_skills/*`）+ MCP 工具（`get_field_spec/search_ocr/validate_candidate_fields/save_result_json`）
  给了模型「自查证据、自校验」的能力，理论上比单轮 LLM 召回更好。

### 5.2 但当前封装会拖垮闭环的「可靠性 + 吞吐 + 成本」（这些才是真正要解决的）
全部已在 `CLAUDE_AGENT_抽取流程问题清单.md` 详述，与闭环直接相关的：
- **吞吐**：A1 同步 subprocess 阻塞事件循环、A2 校验失败全量重跑（×2 时间×2 token）、
  C1 公平调度器放行 4 个但 claude-code 队列单并发 → 病历夹/CRF 批量更新可能跑数小时。
- **可靠性**：A3 单字段不合格丢整表单、A4 turn 耗尽 ParseError 且不自动重试、A5 失败现场被清理难排障。
- **正确性**：B1/B2 合并依赖模型锚点逐字一致（= F2），E2 靶向抽取漏 anchor。
- **成本**：C5/D4 同文档按表单/按 schema 切多个 job，重复投喂整份 OCR。

### 5.3 判断
> **可以用 Claude Code 完成闭环，但不能用「每个 (文档×表单) 起一个一次性 CLI 进程 + 全量重跑 + 单并发队列」这套封装。**
> 闭环的功能件（证据/历史/合并/版本/红框）大部分已就绪，瓶颈在抽取引擎的工程化。建议的引擎层改造：
> 1. **一次调用产出多表单**：同一文档的多个目标表单合并为一次 Claude 调用，省 token、保跨表单上下文、利于合并锚点齐全（解 C5/D4/E2/F2 一大半）。
> 2. **增量修复而非全量重跑**：repair 只回传失败字段 + 错误（解 A2）。
> 3. **逐字段容错落库**：单字段不合格丢该字段、其余照写（解 A3）。
> 4. **异步可取消子进程**：`asyncio.create_subprocess_exec` + 取消轮询（解 A1）。
> 5. **保留降级**：ParseError/超时自动降级到 `LlmEhrExtractor`，不要直接永久失败（解 A4）。

---

## 6. 任务队列 / 会话管理方案

### 6.1 现状梳理
- 队列：Celery，broker=Redis。queue 划分 `ocr / metadata / extraction / claude-code / maintenance`（`celery_app.py:7-11`）。
  claude_code 策略下所有 schema 抽取 job 路由到 `claude-code` 队列（`extraction_strategy.py:53-59`），
  compose 里 `worker-claude-code --concurrency=1`（`docker-compose.prod.yml:198`）。
- 公平调度：`schedule_pending_extraction_jobs` 按 global=4/user=1/project=2 选 job，
  beat 周期触发；把 `queued+running` 都算 active（`extraction_service.py:1243-1279`）。
- 会话：每个 job 一个临时 workspace + 一个 Claude session（`--session-id` 用 run_id，`--no-session-persistence`），
  跑完 `rmtree`（`claude_code_runner.py:146-154,231-237,142-144`）。

### 6.2 核心矛盾
**调度器的并发额度（4）与执行队列的真实容量（1）脱节**（C1）：放行 4 个 job 全堆在单并发队列里排队，
其中 3 个 `queued` 占着全局额度却没在跑，既不能提速又挤掉别的 user/project。

### 6.3 推荐方案

**A. 让调度额度对齐队列容量（最小改动，先做）**
- 把 `EXTRACTION_GLOBAL_CONCURRENCY` 与 claude-code 队列的实际并发对齐，或调度器按「running」而非「queued+running」计 active
  （改 `list_active_for_scheduler` 的状态集合 / `_choose_jobs_for_fair_dispatch` 的计数口径，`extraction_service.py:1247,1143-1145`）。
- 这样「全局 4」才真正意味着「同时在跑 4 个」，user/project 限额才有意义。

**B. 扩 claude-code 队列容量（要钱要算力，按预算做）**
- 多副本 `worker-claude-code`（每副本 `--concurrency=1` 保持单进程稳定）或单副本提并发，
  受限于：① Claude API/账号速率额度，② 每个 Claude 进程的内存/CPU，③ 同 context 写入串行（B5 的 advisory 锁已保证安全）。
- 关键：**并发上限由 Claude 额度决定**，先压测单进程稳定 RPS/超时率再定副本数。

**C. 抽取引擎层减负（治本，和第 5.3 一起做）**
- 一次调用产出多表单（C5/D4）：把「(文档×表单) job 数」从「表单数」降到「文档数」，队列压力直接除以表单数。
- 增量修复（A2）：去掉「失败即整段重跑」的 ×2。

**D. 入队语义统一 + 批量原子性**
- EHR folder 与 CRF folder 的 `enqueue_async` 默认值统一（D3），都走调度器或都直接入队，避免两套行为。
- 批量入队失败要收集失败清单回传、batch 计数如实反映（C3），别让「计划 N 实际 M」对用户不透明。
- 靶向单 job 默认异步（E1），不要在 HTTP 请求里同步跑 Claude（nginx 180s 会先超时但后台仍写库）。

### 6.4 会话（session/workspace）管理
- **失败保留现场**：失败时不 `rmtree`，把 workspace（task.md/input/stdout/result.json）归档到对象存储或保留 N 小时（解 A5）。
  workspace_root 当前在 `/tmp`（`config.py:80`），生产建议挂持久卷或独立目录并配清理策略。
- **超时与取消**：子进程改 `asyncio.create_subprocess_exec` + `wait_for`，worker 侧周期回查 job.status==cancelled 后
  `terminate()`（解 A1），避免取消后还空跑最长 ~600s。
- **session-id 复用**：当前 `--session-id=run_id` + `--no-session-persistence`，每次全新会话、无跨 job 复用，
  对「同文档多表单一次调用」改造后，一个文档一个 session 即可，天然契合方案 C。
- **幂等键**：job 重投时（Celery `max_retries=3`）`process_existing_job` 已用 `_ensure_can_process` 拦截终态重复执行
  （`extraction_service.py:2003-2011`、`extraction_tasks.py:34-42`），保留即可。
- **背压**：claude-code worker `--prefetch-multiplier=1`（已设），保证单进程不预取堆积，配合方案 A 的额度对齐即可。

---

## 7. 落地优先级（按对闭环的阻塞程度）

1. **P0 数据正确性黑洞**：A3（单字段丢整表单）、C2（empty 文档增量跳过）、G3（换版本 context 不迁移）、F1（实例标签不一致）。
   —— 不修，用户会看到「抽不出/填充率清零/实例名混乱」。
2. **P0 吞吐**：C1（额度对齐，方案 6.3-A）+ A2（增量修复）+ 5.3-①（一次多表单）。—— 不修，病历夹/CRF 批量跑不动。
3. **P1 合并质量**：B1/B2/F2（合并键脆弱）、F3（repeat_index 推断）。—— 决定 `_2/_3` 是否正确。
4. **P1 并发安全**：H2（候选切换/抽取写入行锁）、G4（CRF context 唯一约束）。
5. **P2 体验/一致性**：H1（删除降级）、H4/H5/H6（高亮与红框边界）、G1/G2（版本号与发布同步）、D1（协作漏抽）、E1（靶向异步）。

> 说明：本文结论均来自代码静态审查，未运行系统或查生产日志验证实际触发频率。
> 标「高危/已确认」的（F1 双格式、G3 版本跳过、C1 队列脱节、dead code `_resolve_output_record`）已用 grep/读码核实代码事实；
> 部分并发类（H2、G4）属「条件具备即发生」，实际影响取决于运行时并发与数据。




