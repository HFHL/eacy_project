import { emptySuccess } from './_empty'

const request = {
  get: async () => emptySuccess(null),
  post: async () => emptySuccess(null),
  put: async () => emptySuccess(null),
  patch: async () => emptySuccess(null),
  delete: async () => emptySuccess(null),
}

export default request
